{
    "name": "royale-alchemist-web-client",
    "version": "0.1.0",
    "description": "Royale Alchemist web client interface",
    "private": true,
    "homepage": "https://github.com/spicecat/royale-alchemist-web",
    "dependencies": {
        "gh-pages": "^4.0.0",
        "react": "^18.2.0",
        "react-router-dom": "^6.3.0",
        "react-scripts": "^2.1.3",
        "superagent": "^8.0.0"
    },
    "scripts": {
        "start": "PORT=3000 react-scripts start",
        "dev": "PORT=3000 react-scripts start",
        "build": "react-scripts build",
        "predeploy": "npm run build",
        "deploy": "gh-pages -b gh-pages -d build"
    },
    "eslintConfig": {
        "extends": [
            "react-app"
        ]
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}